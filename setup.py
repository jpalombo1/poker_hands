from setuptools import setup  # type: ignore

setup(
    name="poker",
    version="1.0",
    description="Poker Game Hands",
    author="Joseh Palombo",
    packages=["poker"],
    setup_requires=[],
)
